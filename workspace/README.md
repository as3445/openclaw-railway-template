# Workspace

This directory is populated at runtime from `WORKSPACE_REPO` if configured.
Set the `WORKSPACE_REPO` env var to a git repo URL containing your workspace files.
If `GITHUB_TOKEN` is also set, it will be used for authentication.
