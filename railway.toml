[build]
builder = "dockerfile"

[deploy]
# healthcheckPath = "/setup/healthz"  # TEMP: removed to allow shell-in diagnosis
healthcheckTimeout = 900
restartPolicyType = "on_failure"

[variables]
PORT = "8080"
